#![allow(unused)]

#[inline(always)]

fn sbi_call(which: usize, arg0: usize, arg2: usize) -> usize
{
	let ret;
	unsafe{
		llvm_asm
		("ecall"
		:	"={x10}" (ret)
		:	"{x10}" (arg0), "{x11}"(arg2), "{x17}"(which)
		:	"memory"
		:	"volatile"
		);
	}
	ret
}
